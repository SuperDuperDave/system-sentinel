# What the studio page may claim about System Sentinel

Every claim the page at mainthread.ai/work/system-sentinel/ could make about the tool after the overhaul, each with the source path that implements it or the command that checks it. Written 2026-09-20 for the studio's evidence note, which binds the page's words; the page is built in the studio, not here. "Check" commands run from the repository root on the Windows machine (or WSL beside it) with the server running and `TOKEN` set from `system-sentinel token`.

## The boundary

| Claim | Source or check |
| --- | --- |
| One API is the product's single boundary; the desktop dashboard, a phone and a local agent are three clients of it | `sentinel/app.py` (the routes), `dashboard/src/api.ts` (the dashboard is a client over relative paths), `sentinel/mcp_server.py` (the agent's projection of the same catalog) |
| Every reading returns one envelope whose outcome says whether the machine was observed: `ok`, `empty`, `failed`, `unavailable`, `denied`, `timeout` | `sentinel/bridge.py` (`OUTCOMES`), `sentinel/reading.py` (`Reading`); check: `curl -H "Authorization: Bearer $TOKEN" http://127.0.0.1:8000/api/readings/events?count=1` and read `outcome` |
| A collection failure is distinguishable from no findings; `events`, `record` and `faults` also separate an empty match from the retained log's reach and from a response cap | `tests/test_bridge.py`, `tests/test_reading.py::test_empty_window_before_retention_is_not_a_quiet_machine`, `tests/test_host.py::test_record_before_a_moment_and_before_the_log_began`, `::test_a_log_that_does_not_exist_is_a_failure_not_an_empty_result` |
| Raw, derived, invariant and inferred are typed on the wire; derived and inferred sections carry their basis. The selected current hardware inventory is derived because its versions can change and its components are chosen by rules | `sentinel/reading.py` (`Section.cls`, `basis`); check: `.../api/readings/hardware` shows `fingerprint:derived`, `config:raw`, `risks:inferred` |
| Machine gives a visual index over the selected hardware and the current system snapshot without an extra host query; each part opens the matching detailed section, and the exact fields remain below | `dashboard/src/views/MachineOverview.tsx`, `dashboard/src/views/Machine.tsx`; check the Machine view at 390 and 1440 pixels and `.../api/readings/hardware` plus `.../api/readings/system` |
| Signals shows the distribution of leads across five rule families and which input readings answered, with direct links to each class's exact evidence; a zero-lead class whose content inputs did not all answer is qualified on its card, and a zero count is never a health grade | `dashboard/src/views/Signals.tsx`, `sentinel/readings/diagnostics.py` (`method.class_content_inputs`); check the Signals view at 390 and 1440 pixels and `.../api/readings/signals` |
| A dashboard view or record moment can be reopened by its address; browser back/forward restores it, the device sign-in hash still opens its dialog, and a linked section can load after its reading | `dashboard/src/store.ts`, `dashboard/src/App.tsx`; check `?view=machine`, a moment link from Crashes, and `?view=signals#signal-transitions` in Edge |
| Every reading carries its method: a query or a local source, so its evidence can be inspected | `method` in every envelope; check `method.query` on `load` and `method.source` on `performance_history` |
| Thirty-three readings | `GET /api/readings` (the catalog); `sentinel/readings/*.py`; check: `./.venv/bin/python -c 'import sentinel.readings, sentinel.reading as r; print(len(r.REGISTRY))'` |
| The record before a moment: the log does not announce a freeze, the next start does | `sentinel/readings/events.py` (`record`), `dashboard/src/views/Record.tsx` ("The record before this"); check: `.../api/readings/record?before=<a Kernel-Power 41 TimeCreated>&count=25` |
| Record also shows the first System entries at or after a held moment, with its own exact oldest-first query, limit and coverage; the exact boundary belongs only to this side | `sentinel/readings/events.py` (`events.order=oldest`), `dashboard/src/views/Record.tsx` (`After`); `tests/test_exact_log_window.py::test_native_system_moment_split_keeps_exact_boundary_only_on_the_after_side`; check Record on the synthetic screen fixture and `.../api/readings/events?log=System&levels=&since=<an ISO moment>&order=oldest&count=25` |
| One call assembles returned unplanned-stop evidence: `crash` composes each established stop from its session — Windows' own stop estimate when EventLog 6008 supplies a valid one, the next start, a named bug check, a matched dump and the last System record before that start — in one PowerShell launch. A 6008 without a 41 is placed only when its estimate fits returned starts and any previous clean shutdown; an unplaceable marker remains raw with a warning. The last record may be later than the estimate and does not establish cause | `sentinel/readings/crash.py`, `tests/test_crash.py` with `tests/fixtures/crash-records.json`; check: `.../api/readings/crash?count=5` → `stops:derived`, and `method.launches` |
| Given a moment, `crash` reports what the first start at or after it announced when System retention can establish that start; a clean start is `empty` only with complete stop queries and Application retention reaching before it. When retention leaves the first start unknown, surviving report-only and startless stops stay visible and structured coverage names the gap | `sentinel/readings/crash.py`; `tests/test_crash.py::test_a_moment_before_system_retention_keeps_report_only_and_startless_stops`, `::test_proven_system_retention_excludes_the_session_before_a_moment`, `::test_application_retention_must_reach_a_clean_start_before_it_is_called_clean`; check: `.../api/readings/crash?moment=<an ISO moment>` → `coverage:derived` |
| The decisive facts are decoded from the records' own properties, under the names this build's event manifests give them: the bug check code and its parameters from the event's fields, the stop time from EventLog 6008's binary value read as two SYSTEMTIME structures rather than from its locale text. A code the table does not name keeps its number and no name, and a record that does not fit its map says so and keeps its raw form | `sentinel/readings/crash.py` (the field maps, `BUGCHECKS`, `EXCEPTIONS`, `DECODED_BASIS`); `tests/test_crash.py::test_the_6008_stop_time_is_read_from_the_binary_value_not_the_locale_text`, `::test_a_code_the_table_does_not_know_keeps_its_number_and_no_name`, `::test_a_record_that_does_not_fit_its_map_says_so_and_still_reads_what_it_can` |
| `faults`: the programs that crashed or hung and the kernel's own live reports, each with its application, module and exception named, one entry per report among the records returned by that reading | `sentinel/readings/crash.py` (`take_faults`); `tests/test_crash.py::test_each_fault_is_named_with_its_application_its_module_and_its_exception`, `::test_a_live_kernel_event_is_one_entry_per_report_not_one_per_record`; check: `.../api/readings/faults?count=30` |
| Around a held moment, Application fault reports can be inspected before and at/after it through independent newest/oldest reads with an exact shared boundary. A capped oldest read names the exclusive later reach from its probe; invalid clocks or ordering cannot claim completeness. A live-kernel report spanning the split is only grouped from each side's returned records | `sentinel/readings/events.py` (`oldest_window_coverage`), `sentinel/readings/crash.py` (`faults_script`, `take_faults`), `dashboard/src/views/Record.tsx` (`FaultSide`); `tests/test_crash.py::test_oldest_fault_window_uses_a_probe_for_exclusive_later_reach`, `tests/test_screen_fixture.py::test_screen_fixture_keeps_faults_on_both_sides_of_a_crowded_restart`; check Record's nearby Application reports on the crowded fixture |
| A window starts with a broad indexed Event Log query, then filters exact ticks before its row cap: `since` on `events` and `faults` takes an ISO moment or the word `boot`, and anything else is refused before the machine is asked | `sentinel/readings/events.py` (`window_clauses`, which `faults` imports); `tests/test_reading.py::test_events_since_a_moment_and_since_boot`, `tests/test_exact_log_window.py::test_native_exact_filter_excludes_boundary_rows_before_the_count`, `tests/test_crash_observation.py::test_last_record_before_start_keeps_a_predecessor_in_the_same_millisecond`, `tests/test_host.py::test_events_since_boot_holds_to_the_machines_own_idea_of_its_start`; check: `.../api/readings/events?since=yesterday` → `422` |
| `reliability`: Windows' reliability related events include informational records such as successful updates. Its hourly stability index and events are rolled up by UTC day, with the last and lowest index and counts by source and event ID. A missing day is unknown, not zero | `sentinel/readings/reliability.py`; `tests/test_diagnostics.py::test_the_day_rollup_reads_the_index_at_each_days_end_and_its_lowest_hour`, `::test_a_machine_windows_kept_no_record_of_is_empty_not_ok`, `::test_one_class_answering_and_the_other_not_is_still_an_observed_reading`; check: `.../api/readings/reliability?days=30` → `days:derived` |
| What a reading costs is what its query costs, not what starting a shell costs: both transports are measured on the machine and published side by side, so the difference is a table rather than a claim | `docs/measurements/latency.md` (taken 2026-09-21 on the machine in `docs/MACHINE.md`); check: `system-sentinel bench --transport session` then `--transport one-shot`, which rewrites only its own section |
| The tool measures itself: each automatically selectable reading's own `took_ms` over N runs, min, median and p95 nearest-rank, with a reading that was not observed carrying its outcome instead of a time. A reading that needs an exact selection is excluded from the default bench and marked `not taken` if explicitly named without a reference | `sentinel/bench.py`, `sentinel/reading.py` (`requires_selection`); `tests/test_bench.py` |
| Questions reach Windows through a pool of live PowerShell sessions rather than a process per question, so a reading costs what its query costs and not what starting a shell costs; the pool is the only bound on how many questions are in flight, a session that overruns or dies is replaced, and a question that no session can take is launched the old way rather than lost | `sentinel/bridge.py` (`Session`, `Pool`, `classify` shared by both transports); `tests/test_bridge.py` (every outcome asserted through both transports); check: `system-sentinel bench --transport session` against `--transport one-shot` |
| Starting a session is still a launch, and under WSL launches take a slot shared across every process that uses the bridge, so a suite beside a live dashboard does not trip the interop layer | `sentinel/bridge.py` (`WSL_LAUNCH_SLOTS`, taken around session startup); `tests/test_bridge.py::test_under_wsl_launches_share_a_few_slots_across_processes` |
| `health` says how questions are reaching the machine: the transport, sessions alive and idle, questions answered, sessions discarded by reason, the oldest session's age, and how many questions fell back to a launch. Counts, never a verdict | `sentinel/readings/health.py`, `sentinel/bridge.py` (`sessions_report`); check: `.../api/readings/health` → `sections[0].data.bridge.sessions` |
| WHEA reads System WHEA-Logger and Kernel-WHEA/Errors separately, preserving per-source outcome and cutoff. The newest-record list is a bounded preview with fixed-header severity, previous-session flags and log references, without full payloads or decoded structures; selecting a record takes `whea_record` for full fields, a structural CPER check and System decoder detail. Default responses withhold even the projected CPER header bytes; the authenticated screen offers full bytes on demand. Kernel detail decoding remains deferred until channel payload safety is exercised in isolation | `sentinel/readings/whea.py` (`WHEA_SOURCES`, `whea_source`, `record_identity`, `decode_all`), `sentinel/redact.py`, `dashboard/src/views/Errors.tsx`; `tests/test_whea.py`, `tests/test_redact.py` (source failures and caps, preview bounds, CPER identity, ambiguous event ids, decoder boundary, byte redaction, host channel) |
| An agent can request one retained WHEA event by its log-local RecordId even when it falls outside the newest-500 reading. A missing row is distinct from a query failure or ambiguous result, binary properties are bounded before serialization, and default output withholds the full CPER payload but keeps eight header time bytes in the derived identity | `sentinel/readings/whea.py` (`whea_record_script`, `take_whea_record`), `tests/test_whea.py` (synthetic outcomes, authenticated redaction, real bridge exact lookup and binary bound) |
| An agent can ask either WHEA log for bounded previews in an exact seven-digit filing-time window, keeping the newest or oldest side, with that log's own cap probe, retained reach and future end. The preview has no full payload, decoder, cross-log group or signature; a selected reference leads to `whea_record`. A failed source gives no evidence sections, and an empty future window cannot claim completeness | `sentinel/readings/whea.py` (`whea_window_script`, `take_whea_window`, `whea_window_reach`), `tests/test_whea_window.py` (source contract and native PowerShell synthetic tick boundaries) |
| A failed Windows IP-configuration join does not discard observed network adapters or imply they have no addresses: the reading carries a warning and `ip:null` for each affected adapter | `sentinel/readings/system.py` (`NETWORK_SCRIPT`); `tests/test_system.py::test_network_keeps_adapter_inventory_when_windows_cannot_assemble_ip_configuration` |
| One selected dump's bounded structural metadata is read on demand. The raw header bytes and offsets are available beside the interpretation; a recognized x64 kernel header yields its recorded bug check, while an MDMP directory yields listed streams, exception and system metadata, bounded module records, and a range match for the exception address where possible. A protected file says `denied`; a zero-byte or malformed file says why its data is incomplete. The rest of the dump and its cause are not assessed | `sentinel/readings/dump_header.py`, `dashboard/src/views/Crashes.tsx`, `tests/test_dump_header.py`; observed on this host: a readable live kernel report, Windows denial and zero-byte files; synthetic MDMP with module range verified through the Windows bridge |
| Storms: System WHEA-Logger filing times in 60-second buckets, grouped by signature, computed from the log on demand; a live window applies a burst threshold of 5 per minute (critical above 10) and acceleration at 2x a 240-bucket baseline over 10 buckets. The bounded fixed CPER-header projection adds PreviousError and unreadable counts, plus a separate three-valued lead for reports not marked earlier-session; it does not date error occurrence. An exclusive `before` can inspect a past window with exact host query time and observed upper reach but omits live status. `references=true` adds one bounded reference per returned in-window report, including an unplaceable row, after the caller narrows the window; the default omits that extra list and uses sparse bucket columns; a large number of distinct signatures can still make it large. Missing retained history or a stopped query renders as unknown rather than idle; returned valid records remain visible, and an unreadable time prevents false zero buckets | `sentinel/readings/whea.py` (`storms`, `status`, `report_reference`), `sentinel/readings/event_coverage.py`; `tests/test_whea.py`, `tests/test_stack.py` with `tests/fixtures/whea-records.json` |
| Kernel-WHEA report times are a separate bounded reading and timeline with independent channel retention and query outcome. A fixed CPER-header PreviousError flag marks errors from an earlier session; clustering reports after a restart is never called a hardware-error burst. Missing header or time stays unknown and cannot certify a quiet bucket. The broad timeline omits per-report references unless requested; the screen uses `whea_window` for up to 500 previews of a selected stretch, keeps the previous previews visible during that request, and requires source, RecordId and timestamp agreement for exact re-read | `sentinel/readings/whea_reports.py`, `sentinel/readings/whea.py` (`whea_window`), `sentinel/stack.py` (`_bucket_rows`), `dashboard/src/views/Errors.tsx`, `dashboard/src/views/KernelReports.tsx`, `dashboard/src/views/Record.tsx`; `tests/test_whea_reports.py::test_report_references_are_opt_in_without_losing_aggregate_facts`, `tests/test_screen_fixture.py`, saved-Stack tests and synthetic browser checks |
| A whole Kernel-WHEA report timeline defaults to a bounded Stack handoff with channel coverage, PreviousError and unreadable-header counts, selected report-time buckets and first/last reports; the full stored reading remains available | `sentinel/stack.py` (`_report_handoff_sections`), `tests/test_stack.py` |
| A whole storm reading defaults to a bounded agent handoff summary with coverage, source outcome, timeline highlights, fixed-header counts, returned signature counts and exact sample references. When references were requested, the summary counts their omissions while full verbosity retains all of them. Live readings carry report-traffic status and a separate not-marked lead; historical readings explain why no live status is inferred. The person or agent can switch the item to full for every bucket and sample | `sentinel/stack.py` (`new_item`, `_storm_handoff_sections`), `tests/test_stack.py::test_a_week_of_storm_buckets_has_a_bounded_default_handoff_with_full_evidence_available`, `tests/test_stack.py::test_historical_storm_handoff_preserves_anchor_and_coverage_without_live_urgency` |
| The present PCI inventory remains available when parent relations are incomplete; only fully reported chains form groups, and only root-port groups imply a shared PCIe link | `sentinel/readings/diagnostics.py` (`pcie`, from `Get-PnpDevice` and `pnputil /enum-devices /relations`); check: `.../api/readings/pcie` → `devices:raw`, `groups:derived`, `coverage:derived`, `collection:raw` |
| Five forensic signal classes, each a lead with its rule and the readings it drew on, never a diagnosis; pressure names the returned sample instead of calling a high record share a time burst, the display-reset lead cites unique raw System rows instead of the newest unrelated ledger moment, unplanned stops are counted once from Crash, and each input's warning state remains visible | `sentinel/readings/diagnostics.py` (`signals`, `SIGNAL_INPUTS`), `dashboard/src/views/Signals.tsx`; `tests/test_diagnostics.py::test_pressure_share_names_its_returned_scope_without_claiming_a_time_burst`, `::test_display_reset_lead_cites_only_unique_raw_rows_and_no_unrelated_ledger_last`, `::test_the_unexpected_shutdown_signal_counts_crash_stops_once`; check: `.../api/readings/signals` → `method.readings`, `sections[0].basis` |
| Event streaming over server-sent events, with a `bridge` event when a poll did not observe the machine | `sentinel/stream.py`; check: `curl -N -H "Authorization: Bearer $TOKEN" http://127.0.0.1:8000/api/stream` |

## Privacy and the boundary

| Claim | Source or check |
| --- | --- |
| Default responses mask recognized serial and identity fields, learned computer and user names, MAC and network addresses, and user-profile path segments; CPER hex is withheld by content wherever it occurs because its binary sections can carry identifiers. Every current redacted JSON response lists what was removed and which host/user names cannot be masked by value; composed text names the gap when one exists. Unknown identifiers inside free text or other binary formats are not guaranteed to be found. An unexpected identity exception with missing names withholds redacted evidence with a typed HTTP/MCP/stream explanation | `sentinel/redact.py`, `sentinel/app.py`, `sentinel/mcp_server.py`, `sentinel/stream.py`; `tests/test_redact.py`, `tests/test_async_responsiveness.py`, `tests/test_stream.py`; check: `redacted`, `redaction_gaps`, and a sanitized `503 redaction_withheld` on an injected lookup exception |
| A caller receives the real values only by asking for `unredacted` by name | `sentinel/app.py` (`unredacted` query parameter), `sentinel/mcp_server.py` (`unredacted` argument); `tests/test_app.py::test_unredacted_by_name` |
| Every route that carries machine data and the MCP endpoint require the access token or the session cookie, whose value is derived from the token and is not the token | `sentinel/auth.py` (`TokenMiddleware`); check: `curl -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/api/readings` → `401` |
| The server binds to localhost unless told otherwise | `sentinel/cli.py` (`serve --host` default `127.0.0.1`) |
| Machine readings, the handoff, captures and the token are not sent by the updater; handoff is copied to the clipboard and captures are files on disk. A person-triggered update check contacts GitHub for release metadata and an accepted update downloads release assets | `dashboard/src/Copy.tsx`, `sentinel/capture.py`, `sentinel/update.py` |

## For agents

| Claim | Source or check |
| --- | --- |
| An agent registers the server once and every reading is a tool | `docs/API.md` ("Claude Code"); check: `claude mcp add --transport http system-sentinel http://127.0.0.1:8000/mcp --header "Authorization: Bearer $TOKEN"` then list tools |
| Forty-five MCP tools: the thirty-three readings, nine stack tools and three capture tools | `sentinel/mcp_server.py` (`STACK_TOOLS`, `CAPTURE_TOOLS`); check: `tools/list` over JSON-RPC at `/mcp` (see `tests/test_mcp.py`) |
| Every tool says what it does to the machine: the readings and the read-only stack tools are annotated read-only, `stack_remove` and `stack_clear` destructive, and none of them opens onto the world, so a client stops confirming thirty-three harmless readings | `sentinel/mcp_server.py` (one `effect` per tool, annotations derived from it); `tests/test_mcp.py` |
| Every reading answers an agent with typed structured content beside the text, against one shared schema for the envelope, so a client branches on `outcome` and a section's `class` as fields rather than parsing a string | `sentinel/mcp_server.py` (`ENVELOPE_SCHEMA`, `structuredContent`); `tests/test_mcp.py` |
| The prompt library and the catalog are reachable as MCP prompts and resources, not only as tool calls | `sentinel/mcp_server.py` (`prompts/list`, `prompts/get`, `sentinel://catalog`, `sentinel://handoff`); `tests/test_mcp.py` |
| An unredacted view must say why: `unredacted` without a `reason` is refused, and the reason is carried into the answer's warnings | `sentinel/mcp_server.py`; `tests/test_mcp.py` |
| The interface is documented for an agent to read without the source | `docs/API.md`; the live form at `/api/docs` and `/api/openapi.json` |
| An agent reads the same composed handoff a person copies, and needs no clipboard | `sentinel/stack.py` (`compose`), `GET /api/stack/composed`, the `compose` tool |
| A composed Signals handoff carries bounded saved input scope, outcome, time, warning counts and, for a whole reading, per-run class input reach; it marks missing or oversized provenance instead of inventing a class verdict | `sentinel/stack.py` (`_signal_method_handoff`), `tests/test_stack.py::test_one_signal_can_be_handed_on_with_its_basis_and_evidence`, `::test_signals_input_projection_keeps_unknown_and_oversized_held_method_explicit` |
| A one-shot agent connected over HTTP MCP and read the machine through the tool | Observed 2026-09-20: `claude -p` with the server registered read `health` and `events` and reported their outcomes; reproduce with the command in `docs/API.md` |

## The stack and captures

| Claim | Source or check |
| --- | --- |
| The stack lives on the machine, so the desktop, the phone and the agent see one stack | `sentinel/stack.py` (`Store` in the data directory); `tests/test_stack.py` (persistence across two app instances) |
| Items keep the complete reading for the handoff and capture, while routine HTTP/MCP Stack answers carry only item metadata and provenance. One exact item read returns its full saved evidence, so an unobserved reading remains visible without repeatedly transferring every stored section | `sentinel/stack.py` (`Item`, `index_entry`, `Stack.item`, `render`), `sentinel/app.py`, `sentinel/mcp_server.py`, `dashboard/src/views/Stack.tsx`; `tests/test_stack.py` and `tests/test_serialization.py` |
| A Stack index and handoff say whether this server took a reading or a client supplied its envelope; old items keep unknown origin. New envelopes carry the producing version, while supplied version strings remain claims of those envelopes | `sentinel/reading.py` (`Reading.to_dict`), `sentinel/stack.py` (`new_item`, `index_entry`, `_item_lines`), `dashboard/src/views/Stack.tsx`; `tests/test_stack.py::test_stack_names_who_supplied_an_outcome_without_claiming_it_as_an_observation` |
| Six prompt presets by name, yours to add, edit and delete | `sentinel/stack.py` (`PRESET_PROMPTS`); `GET /api/prompts` |
| The same evidence is not stacked twice | `POST /api/stack/items` → `409`; `tests/test_stack.py` |
| A capture holds every reading that needs no exact selection, the stack and the composed text in one ZIP; Signals' seven input members are the observations it used, at its stated scopes, so a cited Crash row can be inspected in the same archive. The manifest names taken members and outcomes, the reused seven's origin and parameters, the redaction applied, value-masking gaps and selection-dependent readings omitted without a false machine failure | `sentinel/capture.py`, `sentinel/readings/diagnostics.py` (`SIGNAL_INPUTS`), `sentinel/reading.py` (`requires_selection`); `tests/test_capture.py::test_capture_crash_references_resolve_inside_its_own_zip`; check a capture's `manifest.json`, `readings/signals.json` and `readings/crash.json` |
| A capture is listed and downloadable only after its ZIP and manifest are complete; concurrent captures keep separate names, and a canceled request leaves no final-looking partial file | `sentinel/capture.py` (`create`, `_publish`); `tests/test_capture.py` (in-flight, concurrent and cancellation cases) |

## The dashboard

| Claim | Source or check |
| --- | --- |
| Ten views: Record, Hardware errors, Crashes, Machine, Performance, Space, Diagnostics, Signals, Stack, Agents | `dashboard/src/store.ts` (`VIEWS`) |
| Space uses one on-demand Windows reading for proportional tiles and a rotatable planet, with a shared selected folder, walk path and exact ranked list; partial folders are lower bounds and unmeasured folders receive no area | `dashboard/src/views/Space.tsx`, `sentinel/readings/space.py`; `tests/test_space.py` covers host allocation, links, limits and opaque drill-down; a synthetic envelope was checked in Chromium at 1440 and 390 pixels for layout and child requests, and at 1280 pixels for a folded page |
| Performance shows local numeric samples across a requested window, leaves missing intervals unconnected, and lets a person inspect a sample with readable units and its full-precision raw row, pause or clear collection, and take a fresh snapshot. A selected sample opens the System record before its timestamp; that sample stays selected on return and a held moment shows its time | `sentinel/performance.py`, `dashboard/src/views/Performance.tsx`, `dashboard/src/store.ts`, `tests/test_performance.py`; check `.../api/readings/performance_history?hours=1`, navigate Performance → Record → Back in Edge at phone and desktop widths |
| Process pressure on opening Performance ranks the fresh Windows process rows by CPU time, private resident memory and process I/O, with an exact searchable row and raw readout; process identity is not stored by the background sampler | `sentinel/readings/processes.py`, `dashboard/src/views/ProcessPressure.tsx`, `tests/test_processes.py`; check `.../api/readings/processes` and inspect the Performance view at phone and desktop widths |
| Crashes answers for a person what `crash` answers for an agent: each stop's separate record, estimate and next-start times lead to its exact detail, record jump and stack action; a report-only stop leads with report time, bug check and dump while naming missing System evidence. Programs, live kernel reports and dump files retain their own outcome lines | `dashboard/src/views/Crashes.tsx`; check `?view=crashes` on a host with both full and report-only stops, select a sequence item by keyboard at 390 px |
| Crash stops do not borrow a module bucket or attached dump from a BlueScreen report whose nonzero code conflicts with their returned Kernel-Power 41 code; the report remains raw and decoded with a warning, and a time-only stop dump match excludes LiveKernelReports files | `sentinel/readings/crash.py` (`_consistent_reports`, `_dump_by_time`), `tests/test_crash.py::test_late_conflicting_report_stays_raw_without_lending_its_bucket_or_dump`, `::test_a_live_kernel_file_alone_is_not_the_time_matched_dump_for_a_stop` |
| A moment is a jump: wherever the dashboard shows a time that frames an investigation — a stop, a fault, a dump file, a hardware-error record, a signal's evidence — one control frames the log around it, and the frame survives leaving the view and coming back | `dashboard/src/store.ts` (`moment`, `setMoment`), `dashboard/src/Sections.tsx` (`MomentLink`), `dashboard/src/views/Record.tsx` (the frame and progressive "more before this" control) |
| The System WHEA-Logger trace is reachable: each lit stretch frames the System log at its end, and an idle stretch is no target. The separate Kernel-WHEA report trace is visual only; its report times are not turned into System-log jumps | `dashboard/src/views/Errors.tsx` (`Trace`) |
| Since boot is a window of its own: Record requests up to 500 records from this session and warns when older matching records remain; Hardware errors rounds the machine's own uptime up to whole hours and says that its window can include time before this session | `dashboard/src/views/Record.tsx`, `dashboard/src/views/Errors.tsx`, `sentinel/readings/events.py` (`since`, `collection`) |
| Every view that takes a reading starts with the outcome line: what was asked, what came back, whether the machine was observed, the method on request (Stack and Agents read no reading; Diagnostics shows it once a reading is taken) | `dashboard/src/Outcome.tsx`; every file under `dashboard/src/views/` |
| Inspect in place: a record opens where it is, and the record before it opens under it | `dashboard/src/views/Record.tsx`, `dashboard/src/Sections.tsx` (`RowList`) |
| Record maps the returned System log sample by time and leading source; each active bar or source opens a matching row below, while blank intervals make no claim about rows outside the sample | `dashboard/src/views/Record.tsx` (`RecordOverview`), `dashboard/src/views/Record.module.css`; Edge desktop and phone jump/focus check |
| On phones, Record summarizes its current level and window in a disclosure; opening it exposes the same level, span and count controls shown directly on desktop | `dashboard/src/views/Record.tsx` (`Log`), `dashboard/src/views/Record.module.css`; Edge 390px closed/opened/changed check |
| After Memory is requested, Diagnostics maps only returned modules, capacity and reported slot population; a failed inventory is visibly unknown. Configured speeds are shown against each module's rating and the full Windows, source-outcome and derived fields stay below | `dashboard/src/views/MemoryMap.tsx`, `dashboard/src/views/Diagnostics.tsx`, `sentinel/readings/diagnostics.py` (`memory_derived`); targeted unknown-field tests and Edge desktop/phone evidence-link check |
| After PCIe is requested, Diagnostics shows relation coverage and reported upstream groups, distinguishes root ports from direct non-PCI parents, and links to the raw PCI device inventory; unavailable topology stays unknown | `dashboard/src/views/PcieMap.tsx`, `dashboard/src/views/Diagnostics.tsx`, `sentinel/readings/diagnostics.py` (`pcie_topology`); Edge desktop/phone group expansion and raw-anchor check |
| Heavy readings are taken only when asked | `dashboard/src/views/Machine.tsx`, `Diagnostics.tsx` ("Take the reading"); `useReading(..., enabled)` |
| Works at phone width over the same boundary | Verified on glass 2026-09-20 at 390x844 and 1440x900, every view, no horizontal overflow; `docs/screens/` holds the sanitized captures with the commit, sizes and the fixture pipeline that reproduces them |
| The identity is in the tool: the tokens, Azeret Mono, JetBrains Mono, DM Sans self-hosted under the OFL, the mark, the graticule in one zone, the phosphor as light only | `dashboard/src/identity.css`, `dashboard/src/Mark.tsx`, `dashboard/src/App.module.css`, `dashboard/public/fonts/`; owned by `docs/design/IDENTITY-DIRECTIONS-2026-09-20.md` |
| One lit word: `live` after Sentinel returns its latest Health reading; `checking`, `issue` and `offline` stay unlit and say why it is not yet live. The nearby readout states the bridge outcome; hidden tabs check again when shown | `dashboard/src/Live.tsx` |

## Running it

| Claim | Source or check |
| --- | --- |
| Runs on the machine it reads, Windows 10 and 11, no hosted version, no account | `README.md`, `sentinel/cli.py`; the user-triggered updater in `sentinel/update.py` is the one external client |
| One process serves the API, the dashboard and the MCP endpoint on one origin | `sentinel/app.py` (`create_app`: routes, `/mcp`, static) |
| A test suite: unit tests through a fake bridge, host tests against the real event log | `tests/`; check: `./.venv/bin/python -m pytest` |
| `system-sentinel check` proves the bridge before anything is asked of it | `sentinel/cli.py` (`_check`) |
| Published as a GitHub Release: one executable and the list of its SHA-256, downloadable without an account; the download path (download, the checksum against the list, unblock, start, first answer, `health` and `events`) run against the published address on a clean Windows 11 image in Windows Sandbox on 2026-09-21 | https://github.com/SuperDuperDave/system-sentinel/releases/latest; `build/windows/sandbox/run.ps1 -SkipSource -Release <url>`, recorded in `docs/DEPLOY.md` ("What the prompts were tested on") |
| Installable by pasting one prompt to your agent, from the release or from source; the agent inspects what it will run before it installs | `docs/DEPLOY.md` (two prompts); the long prompt run end to end through a one-shot Claude Code agent on 2026-09-20 on this machine (prerequisites present), and step by step on a clean Windows 11 image in Windows Sandbox on 2026-09-21 by `build/windows/sandbox/`, both recorded in its "tested on" section; the short prompt's record is there too |
| Double-click and it runs on a machine with nothing installed: one file, the browser opens on the dashboard already signed in, the mark in the tray with Open dashboard, Sign in another device, Copy address for agents, Start with Windows, a submenu named for the version holding Check for updates and Remove from this computer, and Quit; the person never types or reads the token (Copy address for agents places the agent's registration line, token included, on the clipboard on purpose) | `sentinel/launcher.py`, `build/windows/build.ps1`; observed 2026-09-20 on the Windows side from a local disk (nineteen readings, the decoder inside the bundle, 401 without the token, a second launch opened the dashboard and exited, no listener after Quit) and 2026-09-21 on a clean Windows 11 image in Windows Sandbox with no Python (first answer within ten seconds of the start, `health` and `events` ok); the entries built on the Windows side by `sentinel.launcher.tray_menu` (six and the submenu's two, observed 2026-09-21 with the real pystray), checked by `tests/test_launcher.py` where pystray is present |
| The browser is let in by a one-time code signed with the token and spent once, only from this machine; the launcher's log never carries the code | `sentinel/auth.py` (`mint_code`, `code_valid`, `code_expiry`), `sentinel/app.py` (`GET /api/session/open`, `State.spend_code`); `tests/test_launcher.py` |
| A phone signs in by scanning a QR code from the dashboard or the tray: the machine is asked which address it publishes on the private network, the link carries a five-minute one-time code, and the token is never displayed | `sentinel/link.py`, `sentinel/app.py` (`POST /api/session/link`), `dashboard/src/Devices.tsx`, `sentinel/launcher.py` (Sign in another device); `tests/test_link.py`; observed 2026-09-21 through Tailscale serve on this machine: the route answered `ok` with the published address and the link opened the session once |
| The file installs itself: run from any path but the installed one it copies itself into the data directory and starts that copy, so it can be run from Downloads | `sentinel/launcher.py` (`plan`, `install`, `start_installed`); check on Windows: run a copy from a temporary folder with nothing serving, then `(Get-FileHash "$env:LOCALAPPDATA\SystemSentinel\SystemSentinel.exe").Hash` equals the copy's; observed 2026-09-21 on this machine on a spare port and in Windows Sandbox, recorded in `docs/DEPLOY.md` ("What the prompts were tested on") |
| A newer file double-clicked replaces the installed copy: it asks the running one to quit, takes its place and keeps the token, the stack, the prompts and the captures. It does not downgrade; an older file says a newer version is running and leaves it | `sentinel/launcher.py`; check: `(Invoke-RestMethod -Uri http://127.0.0.1:8000/api/readings -Headers @{ Authorization = "Bearer $TOKEN" }).version` before and after, with the token file unchanged; observed 2026-09-21 on this machine (1.0.2 over 1.0.1, and 1.0.1 refused over 1.0.2) and on the clean image (1.0.1 over the published 1.0.0, which has no quit route and is the one update done by hand) |
| The tool can be stopped over its own boundary, by the token and from this machine only: the session cookie a browser or a phone holds is refused | `sentinel/app.py` (`POST /api/quit`), `sentinel/auth.py`, `sentinel/launcher.py` and `sentinel/cli.py` (each sets the quit callback on `State`); check: the route with the bearer header answers `202 {"quitting": true}` and the listener is gone within a few seconds; the same call with only the cookie answers `401` |
| Which version is installed is in the file, the way Windows keeps it | `build/windows/build.ps1` (the generated version resource), `sentinel/__init__.py` (`__version__`); check: `(Get-Item SystemSentinel.exe).VersionInfo.ProductVersion` |
| Which version is answering is on the catalog, so a caller can tell the running process from the file on disk | `sentinel/app.py` (`GET /api/readings`), `dashboard/src/views/Agents.tsx`; check: `curl -H "Authorization: Bearer $TOKEN" http://127.0.0.1:8000/api/readings` → `version` |
| The tool removes itself: a confirmation that names what goes and what stays, then Start with Windows off, quit, and the data directory deleted after the process has exited. Nothing of it is left outside that directory but the Startup shortcut it removes | `sentinel/launcher.py` (the confirmation and the detached removal); `docs/DEPLOY.md` ("Removing it") states the same scope by hand |
| Check for updates asks GitHub for the latest published release when a person chooses the tray action or CLI command. It asks before download and replacement, checks the asset digest against the SHA-256 list and the bytes received, and has no background poll | `sentinel/update.py`, `sentinel/launcher.py`, `sentinel/cli.py`, `tests/test_update.py`; the 1.1.0 release file's frozen Windows handoff was exercised on the host as recorded in `docs/DEPLOY.md` |
| Every release from 1.0.1 is built by the repository's own workflow and carries a build provenance attestation, beside the SHA-256 list and the digest GitHub computes for each asset | `.github/workflows/`; check: `gh attestation verify SystemSentinel.exe -R SuperDuperDave/system-sentinel`, and `gh release view v1.0.0 --json assets --jq '.assets[].digest'` for the digest GitHub holds |
| Reaching it from a phone is a transport in front of the token boundary; Tailscale recommended, a tunnel documented | `docs/DEPLOY.md` ("Optional: reach it from your phone") |
| Reached from a phone over a private network with HTTPS, the API still refusing anything without the token | Observed 2026-09-20: the dashboard opened on a phone over cellular through Tailscale serve (tailnet only, Funnel off), signed in by a one-time code; `GET /api/readings` over the same path answered 401 without the token |

## Deliberate absences

No live telemetry as a service, no prediction, no full crash-dump analysis (`dump_header` reads bounded structural metadata and reports access denial where Windows refuses), no temperature, voltage or fan telemetry (a decision, stated in `docs/API.md`, "What is not here"), no diagnosed machine, no accuracy, no saved time, no users. A hosted, paired dashboard is planned after the overhaul and is not claimed until it exists.

## The screens

| Claim | Source or check |
| --- | --- |
| The screens show the real dashboard rendering a synthetic record built from Windows' own providers, ids and message templates, so nothing from any real machine appears | `docs/screens/README.md`; reproduce with `docs/screens/fixtures/fixture-server.py` and `capture.cjs` |
| Captured at 1440x900 at 2x and 390x844 at 3x from a recorded commit | `docs/screens/README.md` (the table and the commit) |
