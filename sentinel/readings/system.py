"""The machine: ``system``, ``hardware``, ``hardware.cpu``, ``hardware.gpu``, ``hardware.board``,
``hardware.storage``, ``hardware.network``, ``drivers``, ``dumps``.

Built in phase 2 against docs/API.md from the queries in the old backend
(``backend/collectors/system.py``, ``backend/services/system_info.py``,
``backend/services/domains/hardware/``, ``backend/collectors/dumps.py``).
"""
