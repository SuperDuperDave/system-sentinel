"""Deep diagnostics: ``pcie``, ``power``, ``memory``, ``constraints``, ``signals``.

Built in phase 2 against docs/API.md from the queries in the old backend
(``backend/services/domains/{pcie,power,memory,constraints,forensic_signals}.py``).
"""
