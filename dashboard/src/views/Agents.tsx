/** Agents: how a local agent reaches this machine: where the token is, the one-line MCP registration, the API document. Built in phase 2. */
export function Agents() {
  return (
    <section>
      <h1 className="display" style={{ fontSize: 28, margin: '0 0 10px', lineHeight: 1.1 }}>Agents</h1>
      <p className="readout" style={{ color: 'var(--muted)' }}>Not built yet.</p>
    </section>
  );
}
