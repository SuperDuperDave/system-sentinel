/** Crash dumps: the inventory under the Windows dump locations (raw). Built in phase 2. */
export function Dumps() {
  return (
    <section>
      <h1 className="display" style={{ fontSize: 28, margin: '0 0 10px', lineHeight: 1.1 }}>Crash dumps</h1>
      <p className="readout" style={{ color: 'var(--muted)' }}>Not built yet.</p>
    </section>
  );
}
