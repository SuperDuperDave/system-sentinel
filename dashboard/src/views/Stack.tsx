/** Stack: the chosen evidence, its rank and verbosity, the prompt library, the composed handoff to copy, and captures. Built in phase 2. */
export function Stack() {
  return (
    <section>
      <h1 className="display" style={{ fontSize: 28, margin: '0 0 10px', lineHeight: 1.1 }}>Stack</h1>
      <p className="readout" style={{ color: 'var(--muted)' }}>Not built yet.</p>
    </section>
  );
}
