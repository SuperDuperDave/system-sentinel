/** Hardware errors: the storm status and signatures (derived, inferred) over the WHEA records (raw). Built in phase 2. */
export function Errors() {
  return (
    <section>
      <h1 className="display" style={{ fontSize: 28, margin: '0 0 10px', lineHeight: 1.1 }}>Hardware errors</h1>
      <p className="readout" style={{ color: 'var(--muted)' }}>Not built yet.</p>
    </section>
  );
}
