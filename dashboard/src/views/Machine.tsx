/** Machine: the snapshot, the fingerprint and configuration, the sub-areas (CPU, GPU, board, storage, network) and driver changes, as sections. Built in phase 2. */
export function Machine() {
  return (
    <section>
      <h1 className="display" style={{ fontSize: 28, margin: '0 0 10px', lineHeight: 1.1 }}>Machine</h1>
      <p className="readout" style={{ color: 'var(--muted)' }}>Not built yet.</p>
    </section>
  );
}
