/** Diagnostics: PCIe, power, memory and constraints, each a heavy reading loaded on demand, raw and derived kept apart. Built in phase 2. */
export function Diagnostics() {
  return (
    <section>
      <h1 className="display" style={{ fontSize: 28, margin: '0 0 10px', lineHeight: 1.1 }}>Diagnostics</h1>
      <p className="readout" style={{ color: 'var(--muted)' }}>Not built yet.</p>
    </section>
  );
}
